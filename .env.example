GEMINI_API_KEY=your_gemini_api_key_here
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here
LOCATION="Atlanta, GA 30301"
PORT=3001

# Optional: absolute path for the SQLite database. Set this to your persistent
# volume mount (e.g. DATA_DIR=/data on Railway) so rating trends survive
# redeploys. Leave unset locally to use the ./data folder.
# DATA_DIR=/data
